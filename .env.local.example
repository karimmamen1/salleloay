# Automatically supplied by a Neon integration installed from the Vercel Marketplace.
# For local development, run `vercel env pull .env.local` or paste the pooled Neon connection string.
DATABASE_URL=
